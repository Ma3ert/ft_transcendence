// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Status {
  ONLINE
  OFFLINE
  INMATCH
}

model User {
  id            String       @id @default(uuid())
  username      String        @unique
  email         String        @unique
  avatar        String
  twoFactor     Boolean       @default(false)
  xp            Int           @default(0)
  ladelLevel    Int           @default(0)
  // status        Status
  // FriendsList   FriendsList? @relation(fields: [friendsListId], references: [id])
  // friendsListId String?
}

// model FriendsList {
//   id      String @id @default(uuid())
//   friends User[]
// }
